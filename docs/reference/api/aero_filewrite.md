# aero_filewrite

Pivots a `list[StResult]` into an `AeroDatabase` containing numpy arrays shaped for stability and control derivative lookup tables.

```{eval-rst}
.. automodule:: avl_wrapper.aero_filewrite
   :members:
   :undoc-members: False
   :show-inheritance:
```
