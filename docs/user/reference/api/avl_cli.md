# avl_cli

`avl-aero-tables` CLI entry point. Provides `verify`, `sweep`, and `plot` subcommands driven by a YAML project file.

For usage examples see {doc}`/user/quickstart`.

## API

```{eval-rst}
.. automodule:: avl_aero_tables.avl_cli
   :members:
   :undoc-members: False
   :show-inheritance:
```
