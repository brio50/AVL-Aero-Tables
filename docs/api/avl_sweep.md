# avl_sweep

Top-level orchestrator. Calls `avl_fileread`, `avl_rungen`, `avl_bin`, and `st_fileread` in sequence. Exposed publicly as `avl_sweep()` via `avl_wrapper.__init__`.

```{eval-rst}
.. automodule:: avl_wrapper.avl_sweep
   :members:
   :undoc-members: False
   :show-inheritance:
```
