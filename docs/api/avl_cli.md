# avl_cli

`avl-wrapper` CLI entry point. Provides the `verify` and `run` subcommands by delegating to `avl_bin`.

For usage examples see {doc}`/usage`.

## API

```{eval-rst}
.. automodule:: avl_wrapper.avl_cli
   :members:
   :undoc-members: False
   :show-inheritance:
```
